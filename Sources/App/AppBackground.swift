import SwiftUI

extension Color {
    /// Dark-mode background — slightly lighter than pure black so content doesn't
    /// blend into an OLED-black void.
    static let appBackground = Color(red: 0x1C / 255, green: 0x1C / 255, blue: 0x1E / 255)
}
